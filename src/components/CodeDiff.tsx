/**
 * 提交/文件差异展示：已改为 Monaco 内置 Diff Editor（见 MonacoDiffEditor）。
 * 保留此文件与 `VSCodeDiff` 导出名，避免改动各处的 import。
 */
export { MonacoDiffEditor, VSCodeDiff } from './MonacoDiffEditor'
export type { VSCodeDiffProps } from './MonacoDiffEditor'
